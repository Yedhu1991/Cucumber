package org.junit;

import java.sql.Date;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class MultiLogin {
/*	static WebDriver driver;
	@Before
	public void launch() throws InterruptedException {
		String path = System.getProperty("user.dir");
		System.setProperty("webdriver.chrome.driver",path+"//div//chromedriver.exe");
		driver=new ChromeDriver();
		driver.get("https://www.facebook.com/");
		driver.manage().window().maximize();
		Date d=new Date(0);
		System.out.println(d);
		Thread.sleep(3000);
	}
	@Test
	public void test1() throws InterruptedException {
		
		driver.findElement(By.id("email")).sendKeys("monyedhu@gmail.com");
		driver.findElement(By.id("pass")).sendKeys("134654");
		driver.findElement(By.id("loginbutton")).click();
	}
	@Test
	public void test2() throws InterruptedException {
		Thread.sleep(3000);
		driver.findElement(By.id("email")).sendKeys("clanyedhu@gmail.com");
		driver.findElement(By.id("pass")).sendKeys("134654");
		driver.findElement(By.id("loginbutton")).click();
	}
	@After
	public void after() {
		Date d=new Date(0, 0, 0);
		System.out.println(d);
		driver.quit();
	}*/
}
